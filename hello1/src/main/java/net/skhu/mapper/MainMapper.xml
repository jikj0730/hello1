<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.skhu.mapper.MainMapper">

	<select id="login" resultType="UserDto">
		select * from user where userId=#{userId} and password=#{password}
	</select>
	
	<select id="boardList" resultType="ArticleDto">
		select no, title, date, writer from board
	</select>
	
	<select id="checkId"  resultType="Integer">
		SELECT count(*) FROM user where userId=#{userId}
	</select>
	
	<insert id="userInsert" parameterType="UserDto">
		insert into user (userId, password)
		values (#{userId}, #{password});
	</insert>
	
	<insert id="articleInsert">
		insert into board (title, date, writer, content, userNo)
		values( #{title},#{date},#{writer},#{content},#{userNo} )
	</insert>
	
	<select id="readArticle" resultType="ArticleDto">
		select no, title, date, writer, content, userNo from board
		where no=#{no}
	</select>

	<insert id="addReply" >
		insert into reply (content, date, articleNo, userNo, writer)
		values (#{content},#{date},#{articleNo},#{userNo},#{writer})
	</insert>
	
	<select id="replyList" resultType="ReplyDto">
		select no, content, date, writer, userNo
		from reply
		where articleNo=#{articleNo}
		order by date asc
	</select>
	
	<delete id="deleteReply">
		delete from reply
		where no=#{no}
	</delete>
	
	<delete id="deleteArticle">
		delete from board
		where no=#{no}
	</delete>
	
	<delete id="deleteArticleReply">
		delete from reply
		where articleNo=#{articleNo}
	</delete>
	
	<update id="editArticle">
		update board
		set title=#{title}, content=#{content}, date=#{date}  
		where no=#{no}
	</update>
	
	<select id="replycount" resultType="Integer">
		SELECT count(*) FROM reply where articleNo=#{articleNo}
	</select>
	
</mapper>